# model_server.py polls the model folder's /health with this.
requests>=2.32

# What the vendored client needs -- benchmark/baas/, copied from baas-client.
# Pinned to what that repository's uv.lock resolves, so the copy runs against
# what it was written for.
httpx==0.28.1
tenacity==9.1.4
dacite==1.9.2
yarl==1.24.2
openai==2.45.0
anthropic==0.116.0
