The script in this directory was used to pull the `container_config.yaml`/`mlcube.yaml` from all containers hosted on the MedPerf server and obtain the new format IDs for each one of them, corresponding to manifest digests.

Requires a working MedPerf installation along with dependendicies from requirements.txt